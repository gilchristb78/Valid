package domain.deal;

/**
 * A target in a DealStep.
 *
 * Could be an entire container, a single element in a container, or a range of elements within a container.
 */
public abstract class Target {
}
