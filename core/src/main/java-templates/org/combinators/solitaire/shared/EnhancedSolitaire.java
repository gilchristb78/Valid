package org.combinators.solitaire;

// these are still too many to include all at once.

import ks.common.view.*;
import ks.common.controller.*;
import ks.common.model.*;
import ks.common.games.*;
import ks.client.gamefactory.GameWindow;
import ks.launcher.Main;

/**
 * Because framework is immutable, this enhanced solitaire
 * base class is used as the base for all synthesizes
 * code. It provides a useful repository of helper
 * methods that can be relied upon by the synthesized code
 *
 * Follows Open/Closed principle
 */
public class EnhancedSolitaire extends Solitaire {
    public EnhancedSolitaire() {
        super();
    }

    HashMap<>

    /**
     * Ability to get specific entities by reference
     */
    public Element[] getTableau() {

    }
}