package domain.klondike;

import domain.Constraint;
import domain.constraints.MoveInformation;

/**
 * Possibly become more generic.
 *
 * Determine if redeals are even still possible.
 */
public class RedealsAllowed extends Constraint {

    public RedealsAllowed() {
    }
}
