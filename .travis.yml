env:
  global:
    - GENERATE="runMain org.combinators.solitaire"
matrix:
  include:
    - language: scala
      jdk: openjdk8
      script:
        - sbt compile
        - pwd
        - ls
        - sbt "$GENERATE.fan.FanMain"
        - ls
        - sbt compile
        - sbt "testOnly file:./generated/solitaire/src/main/java/org/combinators/solitaire/fan/*"