env:
  global:
    - GENERATE="runMain org.combinators.solitaire"
matrix:
   include:
       - language: scala
         jdk: openjdk8
         script:
         - sbt compile
         - sbt "$GENERATE.fan.FanMain"
         - sbt compile
         - sbt "testOnly file:./target/solitaire/src/main/java/org/combinators/solitaire/fan/*"
